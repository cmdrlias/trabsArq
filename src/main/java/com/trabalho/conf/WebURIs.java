package com.trabalho.conf;

public interface WebURIs {
    public static final String[] PUBLIC_URI = {
            "/",
            "/index",
            "/jogadores",
            "/equipes",
    };
}
